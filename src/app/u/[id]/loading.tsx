import { PageLoader } from "@/components/wine-glass-loader";

// Covers the public profile page and its per-tasting drill-down.
export default function Loading() {
  return <PageLoader label="Loading profile…" />;
}
