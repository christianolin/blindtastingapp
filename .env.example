NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# platform invite emails: "supabase" (Auth's invite mail, default) | "resend" (not wired up yet)
PLATFORM_EMAIL_PROVIDER=supabase
# label scans only — about $0.01 per scan; see AGENTS.md
ANTHROPIC_API_KEY=
# development only; never read in production
# LABEL_READ_FIXTURE=src/lib/label-scan/__fixtures__/produttori-barbaresco-2018.json
# development only
# NEXT_PUBLIC_FORCE_CAN_SCAN=1
